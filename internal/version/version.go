// Package version holds the application semver (bump for releases).
package version

// Version is the public app version shown in the UI and used in default bake-build output names.
const Version = "0.2.1"
